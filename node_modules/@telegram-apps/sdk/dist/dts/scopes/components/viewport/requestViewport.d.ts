import { ExecuteWithOptions, CancelablePromise } from '@telegram-apps/bridge';
export interface RequestViewportResult {
    height: number;
    isExpanded: boolean;
    isStable: boolean;
    width: number;
}
/**
 * Requests viewport actual information from the Telegram application.
 * @param options - request options.
 * @example
 * const viewport = await request({
 *   timeout: 1000
 * });
 */
export declare function requestViewport(options?: ExecuteWithOptions): CancelablePromise<RequestViewportResult>;
